# Minesweeper-AI
An AI project that implements a Minesweeper solver using logical inference and probabilistic reasoning. The solver models board constraints, deduces safe moves through constraint propagation, and uses probability estimates when deterministic logic is insufficient.
